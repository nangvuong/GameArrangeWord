<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.media.MediaView?>


<StackPane xmlns:fx="http://javafx.com/fxml"
    fx:controller="com.ltm.game_client_v3.views.WelcomeController"
    stylesheets="@welcomeview.css"
    prefWidth="1280" prefHeight="720">

    <!-- 🌌 Background -->
    <ImageView fitWidth="1280" fitHeight="720" preserveRatio="false">
        <image>
            <Image url="@../images/home-bg.jpg" />
        </image>
    </ImageView>

    <!-- 🔹 Màn hình chính -->
    <BorderPane style="-fx-background-color: rgba(0,0,0,0.45);">
        <!-- Top bar -->
        <top>
            <BorderPane styleClass="top-bar">

                <!-- 🔸 Left: sound button -->
                <left>
                    <Button fx:id="soundButton" styleClass="icon-button" />
                </left>

                <!-- 🔸 Center: welcome text -->
                <center>
                    <Label fx:id="welcomeLabel" text="Welcome, Player"
                        styleClass="welcome-label" />
                </center>
            </BorderPane>
        </top>

        <!-- Center content -->
        <center>
            <VBox alignment="CENTER" spacing="25">
                <Label text="GAME SẮP XẾP" styleClass="title" />
                <HBox spacing="20" alignment="CENTER">
                    <Button fx:id="playButton" text="▶ PLAY" onAction="#onPlay"
                        styleClass="primary-button" />
                </HBox>
            </VBox>
        </center>
    </BorderPane>
</StackPane>